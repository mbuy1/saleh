-- ============================================================================
-- DISABLE USER TRIGGERS ONLY
-- ============================================================================
-- الخطأ السابق حدث لأننا حاولنا تعطيل "System Triggers" (مثل Foreign Keys).
-- هذا الأمر يعطل فقط الـ Triggers التي أنشأها المستخدم (User Triggers).

ALTER TABLE public.products DISABLE TRIGGER USER;

-- بعد التنفيذ، حاول إضافة المنتج مرة أخرى.
