# 🔒 دليل الأمان للمطورين

## ✅ ما تم التحقق منه:

1. ✅ ملف `.env` موجود في `.gitignore`
2. ✅ ملف `.env` لم يُرفع إلى Git مطلقاً
3. ✅ تم إنشاء `.env.example` كمرجع
4. ✅ المفاتيح الحالية آمنة (لم تُكشف)

---

## 📋 قائمة التحقق الأمنية:

### قبل كل Commit:
- [ ] تحقق أن `.env` في `.gitignore`
- [ ] شغّل: `git status` وتأكد أن `.env` غير مدرج
- [ ] لا تستخدم `git add .` أو `git add *`
- [ ] استخدم `git add` لملفات محددة فقط

### للمطورين الجدد:
```bash
# 1. انسخ الملف المثال
cp .env.example .env

# 2. املأ المفاتيح الحقيقية
# فتح .env وأدخل القيم من Supabase و Cloudflare

# 3. تحقق أنه غير مُتتبّع
git status  # يجب ألا يظهر .env
```

### كيفية الحصول على المفاتيح:

#### Supabase:
1. اذهب إلى https://app.supabase.com
2. اختر المشروع
3. Settings > API
4. انسخ `URL` و `anon/public key`

#### Cloudflare Images:
1. اذهب إلى https://dash.cloudflare.com
2. Account > Images
3. انسخ `Account ID`
4. Create API Token > انسخ Token
5. انسخ `Account Hash` من URL delivery

---

## 🚨 في حالة كشف المفاتيح:

### الخطوات الفورية:

1. **غيّر Cloudflare Token فوراً:**
   - Dashboard > API Tokens > Delete old > Create new

2. **فعّل RLS في Supabase:**
   ```sql
   -- لكل جدول:
   ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;
   
   -- أضف سياسات:
   CREATE POLICY "policy_name" ON table_name
   FOR ALL USING (auth.uid() = user_id);
   ```

3. **امسح `.env` من Git History** (إذا لزم):
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch .env" \
     --prune-empty --tag-name-filter cat -- --all
   
   git push origin --force --all
   ```

---

## ✅ أفضل الممارسات:

### في Development:
- استخدم `.env` محلياً فقط
- لا ترسل `.env` في أي رسالة أو email
- لا تشارك screenshots تحتوي على مفاتيح

### في Production:
- استخدم Environment Variables
- في Cloudflare Workers: `wrangler secret put`
- في Server: متغيرات البيئة من OS

### في CI/CD:
- استخدم Secrets في GitHub Actions
- استخدم Variables في GitLab CI
- لا تطبع المفاتيح في Logs

---

## 📝 ملاحظات مهمة:

### Supabase ANON_KEY:
- ✅ آمن لاستخدامه في الـ client
- ✅ مصمم ليكون public
- ⚠️ لكن يجب تفعيل RLS لحماية البيانات

### Cloudflare Token:
- 🔴 **سري جداً** - يسمح برفع وحذف الصور
- 🔴 **لا تشاركه أبداً**
- 🔴 غيّره فوراً إذا انكشف

---

## 🛡️ الحماية الحالية:

✅ **المشروع آمن حالياً:**
- `.env` غير موجود في Git
- المفاتيح لم تُكشف
- `.gitignore` يحمي `.env`
- `.env.example` جاهز للمطورين

**حافظ على هذا الأمان!** 🔒

---

**آخر تحديث:** 30 نوفمبر 2025
